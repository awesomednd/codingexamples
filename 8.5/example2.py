valueInput = int(input("Enter a number")) # Setting valueInput to the interger of the inputted value
storedValue = 100 # Setting the value of storedValue to 100
if valueInput > storedValue: # Checking if the value of valueInput is greater than storedValue
    print("It is more than 100") # If it is print It is more than 100