def outputData(dataParameter): # Creating the def for outputData that requiers an input 
    print(str(dataParameter)) # Printing

# Main
data = 1 # Setting the value to 1
print(str(data)) # Printing
outputData(data) # Running outputData with adding the variable data